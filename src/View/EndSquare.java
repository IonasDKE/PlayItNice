package View;

/*import javafx.scene.paint.Color;

import static Controller.Controller.*;

public class EndSquare extends Player {

    public EndSquare(Color color, String name, boolean ai) {
        super(color, name, ai);
    }

    public void play(){
        System.out.println(countSquare());
        Player p = Player.getActualPlayer();

        if (Launcher.pOneAI == true && Launcher.pTwoAI == true) {
            if (p.getTurn() == 1 || p.getTurn() == 2) {
                completeSquare();
            }
        }

        if (Launcher.pTwoAI == true) {
            if (p.getTurn() == 2) {
                completeSquare();
            }
        }

        if (Launcher.pOneAI == true) {
            if (p.getTurn() == 1) {
                completeSquare();
            }
        }
        Player.changeTurn();
    }
}*/
